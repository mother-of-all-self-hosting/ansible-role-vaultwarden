DATABASE_URL={{ vaultwarden_config_database_url }}

DOMAIN={{ vaultwarden_config_domain }}

SIGNUPS_ALLOWED={{ vaultwarden_config_signups_enabled | to_json }}
SIGNUPS_VERIFY={{ vaultwarden_config_signups_verify | to_json }}
{% if vaultwarden_config_signups_domains_whitelist | length > 0 %}
SIGNUPS_DOMAINS_WHITELIST={{ vaultwarden_config_signups_domains_whitelist | join(',') }}
{% endif %}

INVITATIONS_ALLOWED={{ vaultwarden_config_invitations_allowed }}

SHOW_PASSWORD_HINT={{ vaultwarden_config_show_password_hint | to_json }}

REQUIRE_DEVICE_EMAIL={{ vaultwarden_config_require_device_email | to_json }}

ADMIN_TOKEN={{ vaultwarden_config_admin_token }}

ROCKET_PORT={{ vaultwarden_config_rocket_port }}
ROCKET_LIMITS={{ vaultwarden_config_rocket_limits }}

SMTP_FROM={{ vaultwarden_config_smtp_from }}
SMTP_HOST={{ vaultwarden_config_smtp_host }}
SMTP_PORT={{ vaultwarden_config_smtp_port }}
SMTP_SECURITY={{ vaultwarden_config_smtp_security }}
{% if vaultwarden_config_smtp_username and vaultwarden_config_smtp_password %}
SMTP_USERNAME={{ vaultwarden_config_smtp_username }}
SMTP_PASSWORD={{ vaultwarden_config_smtp_password }}
{% endif %}

ICON_CACHE_FOLDER={{ vaultwarden_config_icon_cache_folder }}
SENDS_FOLDER={{ vaultwarden_config_sends_folder }}
TMP_FOLDER={{ vaultwarden_config_tmp_folder }}

LOG_LEVEL={{ vaultwarden_config_log_level }}
EXTENDED_LOGGING={{ vaultwarden_config_extended_logging }}

{% if vaultwarden_config_experimental_client_feature_flags | length > 0 %{
EXPERIMENTAL_CLIENT_FEATURE_FLAGS={{ vaultwarden_config_experimental_client_feature_flags }}
{% endif %}

{{ vaultwarden_container_additional_environment_variables }}
